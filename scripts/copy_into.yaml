---
- name: Copy website folder into image
  hosts: all
  tasks:
    - name: Add website folder
      synchronize:
        src: /home/clement/Documents/website/dev_test/
        dest: /root
        recursive: yes
        rsync_opts:
          - "--no-motd"
